services:
  - type: web
    name: lms-extractor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: etits
        generateValue: true   # Render generates a random value ONCE and keeps it stable
